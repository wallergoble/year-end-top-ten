extends ../layout

block content
    h3= list.title         
    h4= list.description
        ol
        - list.item.forEach( item => {
        li= item
        - })
    button hopefully this edits at some point
    button hopefully this deletes at some point
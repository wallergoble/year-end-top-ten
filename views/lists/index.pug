extends ../layout

block content
    h1 My Lists

    - lists.forEach( list => {        // Nested forEach, consider refactoring later
      .well
        h3= list.title         
        h4= list.description
          ol
          - list.item.forEach( item => {
            li= item
          - })
    - })
//- <a href='/recipes/<%= recipe._id %>' class='btn btn-primary'>More
    a.btn.btn-success(href="/lists/new") Add List

extends ../layout

block content
    h1 My Lists

    - lists.forEach( list => {
      h3= list.title 
      h4= list.description
        ol
        - list.item.forEach( item => {
          li= item
        - })
    - })

    a.btn.btn-success(href="/lists/new") Add List

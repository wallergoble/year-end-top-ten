nav.navbar.navbar-inverse( role="navigation" )
    .navbar-header
        button.navbar-toggle.collapsed( type="button", data-toggle="collapse", data-target="#navbar-default", aria-expanded="false", aria-controls="navbar")
            span.sr-only Toggle navigation
            span.icon-bar
            span.icon-bar
            span.icon-bar
        a.navbar-brand(href="/") YET10

    #navbar-default.collapse.navbar-collapse
        ul.nav.navbar-nav.navbar-right
            li: a( href="/") Root
            - if (currentUser) {
              li: a( href="#")= currentUser.local.email
              li: a( href="/lists/new") #[span.glyphicon.glyphicon-plus(aria-hidden="true")] New List
              li: a( href="/logout") Logout
            - }
            - else {
              li: a( href="/login" ) Login
              li: a( href="/signup" ) Signup
            - }
            li: a( href="/lists" ) Lists

.flash
- if (typeof message !== 'undefined') {
    - if (typeof message.error !== 'undefined' && message.error.length > 0) {
        - console.log('this happened?')
        .bg-danger=  message.error
- }
    - if (typeof message.info !== 'undefined' && message.info.length > 0) {
        .bg-info= message.info
- }
    - if (typeof message.success !== 'undefined' && message.success.length > 0) {
        .bg-success= message.info
    - }
- }
